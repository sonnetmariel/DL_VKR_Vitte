# DL_VKR_Vitte

## Интеллектуальный сервис генерации и обработки изображений животных при помощи нейросетей

Репозиторий содержит экспериментальные материалы и пользовательское Flask-приложение для нейросетевой обработки изображений животных. Проект ориентирован на два основных сценария: восстановление качества изображения и сегментацию объекта животного с последующей обработкой фона или сохранением объекта на прозрачном фоне.

Практическая часть проекта построена на результатах экспериментов с набором Oxford-IIIT Pet. В ходе работы были подготовлены данные, обучены и проверены модели сегментации и восстановления качества, сохранены артефакты экспериментов и реализовано приложение, позволяющее использовать обученные модели через понятный веб-интерфейс.

---

## Возможности приложения

Приложение позволяет:

- загружать изображения животных в форматах `jpg`, `jpeg`, `png`, `webp`;
- выполнять нейросетевое восстановление качества изображения;
- выбирать режим восстановления качества;
- применять сегментацию объекта животного;
- выделять объект на прозрачном фоне;
- размывать или заменять фон на основе сегментационной маски;
- сравнивать изображение до и после обработки;
- формировать текстовый отчет по результатам обработки;
- сохранять историю пользовательских запусков;
- просматривать сведения о системе, моделях, артефактах и настройках.

---

## Основная идея проекта

Проект решает задачу прикладной нейросетевой обработки изображений животных. В качестве исходных данных используются изображения кошек и собак из набора Oxford-IIIT Pet. На основе набора были выполнены два экспериментальных направления:

1. **Сегментация объекта животного**  
   Модель выделяет животное на изображении и формирует бинарную маску. Маска используется для отделения объекта от фона, создания изображения с прозрачным фоном и фоновой обработки.

2. **Восстановление качества изображения**  
   Модель получает искусственно ухудшенное изображение и формирует восстановленную версию. В эксперименте рассматривались разные типы ухудшений: шум, размытие, JPEG-сжатие, изменение яркости и контрастности, уменьшение с обратным увеличением размера, размытие движения и комбинированные искажения.

---

## Результаты экспериментов

### Сегментация

Финальная модель сегментации показала следующие контрольные значения:

| Метрика | Значение |
|---|---:|
| Dice | 0.9332 |
| IoU | 0.8830 |
| Pixel Accuracy | 0.9502 |
| Рабочий размер изображения | 256 × 256 |
| Порог бинаризации | 0.5 |

Такие значения позволяют использовать модель в пользовательском приложении для отделения животного от фона и дальнейшей обработки результата.

### Восстановление качества

В финальном сравнении режимов восстановления лучший результат по структурному сходству показала калиброванная схема с адаптивным режимом:

| Режим | PSNR | SSIM | MAE |
|---|---:|---:|---:|
| Без восстановления | 27.2425 | 0.7875 | 9.9321 |
| Первая остаточная модель | 28.7246 | 0.8440 | 8.6783 |
| Калиброванная схема, общий режим | 28.7246 | 0.8440 | 8.6783 |
| Калиброванная схема, адаптивный режим | 28.7407 | 0.8471 | 8.7431 |

Адаптивный режим выбирает коэффициент нейросетевой обработки с учетом предполагаемого типа ухудшения изображения.

---

## Структура репозитория

```text
DL_VKR_Vitte/
│
├── эксперименты/
│   ├── OxfordPet_ex1.ipynb
│   └── OxfordPet_ex2.ipynb
│
└── image_neural_service/
    ├── app.py
    ├── config.py
    ├── degrade_image.py
    ├── image_utils.py
    ├── llm_client.py
    ├── model_registry.py
    ├── report_service.py
    ├── restoration_service.py
    ├── segmentation_service.py
    ├── storage.py
    ├── requirements.txt
    ├── .env
    │
    ├── artifacts/
    │   ├── segmentation/
    │   └── restoration/
    │
    ├── control_images/
    ├── runtime/
    ├── static/
    └── templates/

Проект использует:
Python;
Flask;
PyTorch;
torchvision;
segmentation_models_pytorch;
timm;
OpenCV;
Pillow;
NumPy;
pandas;
SQLite;
python-dotenv;
ProxyAPI для опциональной генерации текстовых пояснений.

Режимы обработки
Восстановление качества

Поддерживаются режимы восстановления, заданные в конфигурации артефактов. Основной практический сценарий связан с адаптивным выбором коэффициента обработки по предполагаемому типу ухудшения.

Примеры типов ухудшений:

изменение яркости и контрастности;
комбинированное ухудшение;
уменьшение и обратное увеличение размера;
гауссово размытие;
гауссов шум;
JPEG-сжатие;
размытие движения.
Сегментация

Сегментационный модуль формирует маску объекта животного. На основе маски доступны пользовательские сценарии:

выделение животного;
сохранение объекта на прозрачном фоне;
размытие фона;
замена фона;
просмотр маски сегментации.

Контроль качества

Для оценки восстановления качества используются:

MAE - средняя абсолютная ошибка;
MSE - среднеквадратичная ошибка;
PSNR - отношение сигнала к искажению;
SSIM - структурное сходство изображений.

Для оценки сегментации используются:

Dice - коэффициент сходства масок;
IoU - пересечение над объединением;
Pixel Accuracy - пиксельная точность.

Особенности проекта

Проект объединяет исследовательскую и прикладную части. Эксперименты выполнялись в Google Colab, а сохраненные артефакты были перенесены в локальное Flask-приложение. Такой подход позволяет воспроизвести путь от подготовки данных и обучения моделей до пользовательского сценария обработки изображения.

Ключевые особенности:

использование реального набора изображений животных;
разделение экспериментов на сегментацию и восстановление качества;
сохранение обученных моделей и конфигураций инференса;
модульная структура приложения;
локальное хранение пользовательских изображений и истории;
возможность подключения LLM для формирования расширенных пояснений;
ориентация интерфейса на пользователя без специальных знаний в машинном обучении.

Метрики позволяют оценивать результат не только визуально, но и количественно. Это важно для сопоставления моделей, выбора режима восстановления и подтверждения пригодности артефактов для пользовательского приложения.

Ограничения

Текущая версия приложения рассчитана на обработку отдельных пользовательских изображений. Качество результата зависит от исходного изображения, типа искажения, разрешения, положения животного в кадре и наличия артефактов, отличающихся от ухудшений, использованных в эксперименте.

Модели обучались на изображениях кошек и собак, поэтому применение к другим видам животных может снижать точность сегментации и восстановления.

Дальнейшее развитие проекта может включать:

расширение набора данных за счет других видов животных;
добавление моделей сверхразрешения;
улучшение автоматического определения типа ухудшения;
поддержку пакетной обработки изображений;
экспорт отчетов в PDF или DOCX;
добавление пользовательских настроек фона;
контейнеризацию приложения через Docker;
вынесение инференса в отдельный API-сервис.
